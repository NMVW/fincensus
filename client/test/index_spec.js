var assert = require('chai').assert;

describe('Components Spec', function() {
  describe('minor component test', function() {
    it('should just be a happy easy go lucky test', function() {
      assert.equal(33, 33);
      assert.equal('happy', 'happy');
    });
  });
});
