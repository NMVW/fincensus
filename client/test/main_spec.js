var assert = require('chai').assert;

describe('Main Spec', function() {
  describe('minor test', function() {
    it('should just be another happy easy go lucky test', function() {
      assert.equal(33, 33);
      assert.equal('happy', 'happy');
    });
  });
});
